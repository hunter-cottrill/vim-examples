---
description: Scaffold a new Vim Connect app from a short use-case description
argument-hint: [one-line description of what the app should do]
---

The user wants to build a Vim Connect app. Use the build-vim-app skill.

If they gave a description in the arguments, treat it as the `goal` line and fill in the
rest of the use case through the intake questions. If they gave nothing, ask for the goal
first, in one sentence, before anything else.